# Backend base URL (no trailing slash). Locally the Spring Boot app runs on 8080.
NEXT_PUBLIC_API_URL=http://localhost:8080
# Same-origin mode (one public URL): NEXT_PUBLIC_API_URL=/ and API_PROXY_TARGET=http://localhost:8080
